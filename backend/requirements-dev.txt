# Test-only dependencies. Deliberately separate from requirements.txt so none of
# this ships in the production image (backend/Dockerfile installs that one only).
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
pytest==8.3.4
pytest-asyncio==0.25.2

# The transport behind fastapi.testclient.TestClient.
httpx==0.28.1
